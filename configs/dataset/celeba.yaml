dataset:
  name: photos
  root: data/raw/celeba
  metadata_filename: metadata.csv
  image_size: 224
  train_ratio: 0.7
  val_ratio: 0.15
  batch_size: 64
  num_workers: 4
  use_weighted_sampler: false
